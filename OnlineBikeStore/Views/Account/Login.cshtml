@model OnlineBikeStore.Models.LoginViewModel

@{
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}
<link href="~/Content/Customer/login.css" rel="stylesheet" />

@if (ViewBag.msg != null)
{
    <h1 class="alert-warning">@ViewBag.msg</h1>
}

@*<div class="row">
        @if (ViewBag.msg != null)
        {
            <h1 class="alert-warning">@ViewBag.msg</h1>
        }

        <div class="col-12 col-lg-5">
            <div class="card login-card bg-gradient">
                <center><h4>SIGN IN</h4></center>
                @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">
                        <hr />
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            <label class="control-label col-md">Email Id</label>
                            <div class="col-md-auto">
                                @Html.EditorFor(model => model.email, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.email, null, new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="control-label col-md">Password</label>
                            <div class="col-md">
                                @Html.EditorFor(model => model.password, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" })
                            </div>
                        </div>
                        <br />
                        <div class="form-group">
                            <div class="col-md-offset-2 col-md-auto">
                                <input type="submit" value="Login" class="btn btn-success btn-block w-100" />
                            </div>
                        </div>
                    </div>
                }
                <div>
                    Don't have account?  @Html.ActionLink("Register", "Register", "Account") here.

                </div>
            </div>
        </div>
        <div class="col-12 col-lg-7 d-flex">
            <div class="overlay">
                <h2>Your Perfect Ride Awaits!</h2>
                <p>Join us and explore the best motorbikes and gear to fuel your passion for the open road.</p>
            </div>
        </div>




    </div>*@
<section>
    <div class="container py-2">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-xl-10">
                <div class="card" style="border-radius: 1rem;">
                    <div class="row g-0">
                        <div class="col-md-6 col-lg-5 d-none d-md-block">
                            <img src="~/Images/Utilities/login_image.jpg"
                                 alt="login form" class="login-img" style="border-radius: 1rem 0 0 1rem;" />
                        </div>
                        <div class="col-md-6 col-lg-7 d-flex align-items-center">
                            <div class="card-body p-4 p-lg-5 text-black">

                                @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                                {
                                    @Html.AntiForgeryToken()
                                    <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Sign into your account</h5>

                                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                    <div data-mdb-input-init class="form-outline mb-4">
                                        <label class="control-label col-md">Email Id</label>
                                        @Html.EditorFor(model => model.email, new { htmlAttributes = new { @class = "form-control" } })
                                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                    </div>

                                    <div data-mdb-input-init class="form-outline mb-4">
                                        <label class="control-label col-md">Password</label>
                                        @Html.EditorFor(model => model.password, new { htmlAttributes = new { @class = "form-control" } })
                                        @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" })
                                    </div>

                                    <div class="pt-1 mb-4">
                                        <button class="btn login-form-btn" type="submit">Login</button>
                                    </div>



                                }
                                <a class="small text-muted" href="#!">Forgot password?</a>
                                <div>
                                    Don't have account?  @Html.ActionLink("Register", "Register", "Account") here.

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>